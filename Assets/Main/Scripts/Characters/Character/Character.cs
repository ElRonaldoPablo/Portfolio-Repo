using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Character : MonoBehaviour
{
    //[Header("Character Properties")]
    //[SerializeField][Range(0, 1000)] protected int maxHP = 100;
    //[SerializeField][Range(0, 1000)] protected int currentHP = 100;
    //[SerializeField][Range(0.0f, 100.0f)] private float moveSpeed = 10.0f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
